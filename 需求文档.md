# XGPS 楼宇宽带覆盖排查工具 — 需求文档

> 版本：v3.1（人工判定模式） ｜ 更新日期：2026-08-28
> 目标站点：中国移动香港 https://www.hk.chinamobile.com 家居宽频地址查询

---

## 1. 项目背景

中国移动香港官网提供"家居宽频覆盖查询"能力，需要批量排查一批楼宇是否支持 **2500M 宽带安装**（XGS-PON 覆盖）。站点带有「观镜」反自动化防御系统，普通无头浏览器会被拦截，因此采用**有头浏览器持久化上下文 + 反检测参数 + 混合调用**的方案。

## 2. 总体流程（v3 最终版）

```
打开浏览器（有头）
   │
   ▼
输入楼宇地址 → #searchInput（逐字输入触发联想）
   │
   ▼
点击下拉第一个地址（.searchGoodsLine）
   │   ├─ 浏览器自动触发 searchAddress（网络捕获 address_obj）
   │   └─ 点击后自动触发 getAddressDetail
   ▼
【后台】调 getAddressDetail → busiResp 保存为 busiRespObj
   │   → 得到该楼宇所有 floor / flat
   ▼
【采样】每 5 层随机选 1 层 curFloor，当层随机选 1 个 curFlat
   │
   ▼
对每组采样：
   ├─ 重新走一遍 search 流程（保证页面状态干净）
   ├─ 页面下拉中选 Floor / Flat（.el-select）
   ├─ 点击「下一步」#btn_family_001 → 跳转结果页
   ├─ 【人工判定模式 --judge（默认）】等用户在终端输入 Y/N/E/S，没有输入或点击的话必须等待用户输入或点击
   └─ 【全自动模式（默认）】浏览器内 fetch getInstallInfo → 自动判定
   │
   ▼
结果实时落库 output/result.xlsx（行键幂等 upsert）
```

## 3. 接口清单（三个接口各用不同令牌）

| 接口 | 调用方式 | 令牌环境变量 | 用途 |
|---|---|---|---|
| `…/homeBroadBand/searchAddress` | 浏览器输入触发（网络捕获） | `XGIOG_TOKEN_SEARCH` | 地址联想，取第一个地址 |
| `…/homeBroadBand/getAddressDetail` | **后台 requests** | `XGIOG_TOKEN_DETAIL` | 取楼宇全部楼层/单位，`busiResp` 存为 `busiRespObj` |
| `…/homeBroadBand/getInstallInfo` | **浏览器内 fetch**（page.evaluate） | `XGIOG_TOKEN_INSTALL` | 查询该 floor/flat 是否可装 2500M |

令牌通过 CLI 可分别覆盖：`--token-search / --token-detail / --token-install`。

## 4. 采样规则

- 输入：某 BuildingCode 下所有 Floor 及每层 Flat 列表。
- 分组：楼层排序后**每 5 层一组**。
- 每组内**随机选 1 层** → `curFloor`；该层 Flat 中**随机选 1 个** → `curFlat`。
- 随机种子固定：`random.Random("XGPS|" + 楼宇名)` → 同一栋楼多次运行采样结果一致（幂等）。

## 5. 浏览器自动化要点

| 要点 | 说明 |
|---|---|
| 启动方式 | Playwright `launch_persistent_context`，Edge/Chromium 通道，**有头** |
| 反检测 | `--disable-blink-features=AutomationControlled` + 忽略 `--enable-automation` |
| 搜索输入 | `#searchInput`，`press_sequentially(delay=40)` 逐字输入触发 input 事件 |
| 联想下拉 | `.broadband-search .searchGoodsLine`，点击第一项 |
| 楼层/单位 | 两个 `.el-select` 下拉 |
| 下一步按钮 | `#btn_family_001`（`<button data-v-4b000cc3 id="btn_family_001">下一步</button>`） |
| **点击下一步的关键** | 选完楼层/单位后按钮有 ~500ms 的 Vue 异步渲染 + CSS 过渡，必须 `wait_for(state="visible")` 等真正可见，再 `scroll_into_view_if_needed()` + `click()` |
| 多组采样 | 每组**重新走 search 流程**（点完下一步页面已跳结果页，页面状态必须重置）；`address_obj/busiRespObj` 楼宇级缓存不重取 |

## 6. 人工判定模式（`--judge`）

点击「下一步」跳到结果页后，程序在终端暂停，等用户查看浏览器页面后输入或者点击：

| 输入                 | 含义 | 落库 |
|--------------------|---|---|
| `Y`或者点击"可卖2500M"   | 页面显示可卖 2500M | `Is2500Support=Y` |
| `N` 或者点击"不可卖2500M" | 不可卖 2500M | `Is2500Support=N`，`defeatBuilding=Y` |
| `E`或者点击"其他情况待排"    | 异常/看不清 | `Is2500Support=E` |
| `S`或者点击“跳过”        | 跳过该条 | `remark=用户跳过` |

判定依据：结果页展示的 "家居寬頻 1000 / 家居寬頻 2500 / 所有家居寬頻" 商品列表。

## 7. 数据落库

输出：`output/result.xlsx`（openpyxl，每次写入即保存）

| 列 | 说明 |
|---|---|
| inputBuilding | 输入文件中的原始楼宇行 |
| curBuilding | 页面选中的地址文本 |
| curFloor / curFlat | 采样楼层 / 单位 |
| Is2500Support | Y / N / E |
| defeatBuilding | 不可卖时为 Y |
| remark | 判定说明 / 错误信息 |
| buildingCode / ofcaCode / carrier | 楼宇编码等 |
| isXGSPONsupport / coverType | getInstallInfo 返回的覆盖标识 |
| floorGroup | 采样分组（第几组 5 层） |
| checkedAt | 检查时间 |

- **行级幂等**：行键 = `(inputBuilding, curFloor, curFlat)`，upsert 不重复。
- **楼宇级断点续跑**：`output/progress.json` 记录 done 楼宇，下次运行自动跳过。
- 异常路径（搜索无结果 / 无楼层数据）降级记录 remark，不中断全局。

## 8. 容错机制

- **浏览器自动重连**：浏览器被手动关闭或崩溃后 `ensure_alive()` 自动重启 persistent context，单栋最多重试。
- **单栋异常兜底**：任何未预期异常写错误行后继续下一栋，不终止全局。
- **接口节流**：每次调用间 `REQUEST_INTERVAL` 延时。

## 9. 输入输出文件

| 文件 | 说明 |
|---|---|
| `input_buildings.txt` | 41 栋楼宇，一行一个（格式：`座,楼宇,街道,地区,区域`） |
| `output/result.xlsx` | 结果落库 |
| `output/progress.json` | 楼宇级进度 |
| `output/result_v2_backup*.xlsx` | v2 历史数据备份（兜底可恢复） |

## 10. 使用方式

```bash
python main.py                     # 全自动模式（getInstallInfo 自动判定）
python main.py --judge             # 人工判定模式（点下一步后等 Y/N/E/S）
python main.py --limit 5           # 只处理前 5 栋
python main.py --redo              # 忽略已有结果全部重跑
python main.py --token-search X --token-detail Y --token-install Z   # 令牌过期时分别覆盖
```

## 11. 已验证结论（参考）

- 怡景園（26 层）→ 6 组采样全部 `isXGSPON=Y`，可卖 2500M。
- 葵樹樓 → 6 组全部 N，不可卖。
- judge 模式 6/6 组「下一步」点击全部成功，单栋约 1 分 25 秒。
- 采样随机种子绑定楼宇名，`--redo` 不产生重复行。
